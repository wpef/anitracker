{
  "name": "anitracker",
  "version": "1.0.0",
  "private": true,
  "description": "Dog activity tracker PWA",
  "scripts": {
    "build": "node scripts/build.js",
    "sync": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
