.env
.env.*
.DS_Store
node_modules/
*.log
.vscode/
.idea/
thumbs.db

# Capacitor web build output
www/

# Android build artifacts
android/.gradle/
android/app/build/
android/build/
android/local.properties

# Android signing secrets — NEVER commit these
keystore.properties
*.jks
*.keystore
android/app/google-services.json

# Service-account JSON keys (Firebase App Distribution, Play Developer API) —
# NEVER commit these. They live only in GitHub Actions secrets.
*service-account*.json
*-sa.json
firebase-service-account.json
play-service-account.json

# Built APKs/AABs (delivered as artifacts, not committed)
*.apk
*.aab
