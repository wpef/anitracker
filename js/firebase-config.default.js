/**
 * firebase-config.default.js – Bundled Firebase config for store builds.
 *
 * Fill this with YOUR Firebase project config so end users NEVER see the
 * "paste config" setup screen — the app boots straight to the login page.
 *
 * Where to find it:
 *   Firebase console → Project settings → "Vos applications" → SDK setup → Config.
 * Minimum required keys: apiKey + databaseURL.
 *
 * Leave the object empty to keep the dev/self-host flow (manual setup screen).
 */
export const DEFAULT_FIREBASE_CONFIG = {
  // apiKey:       "",
  // authDomain:   "",
  // databaseURL:  "",
  // projectId:    "",
  // storageBucket:"",
  // appId:        "",
};
